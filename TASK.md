# 赛题六：基于 CANN 的分布式通信算法设计与优化

## 1. 题目背景
近年来，大语言模型与多模态基础模型的参数量呈指数级扩张，从百亿、千亿迈向万亿级别，其复杂的深度神经网络架构与稀疏混合专家 (MoE) 设计进一步加剧了计算与存储压力。单卡算力与显存已远不足以支撑大规模训练与推理，必须依托数据并行、张量并行、流水线并行及专家并行等多维分布式策略，将模型参数、梯度与激活值拆解至数百乃至数千张 GPU 组成的高性能集群中协同执行。

在此过程中，集合通信操作的开销急剧膨胀，通信耗时在单轮迭代中的占比从早期小模型的不足 10%，攀升至千亿级模型训练的 30%-50%，在 MoE 架构与千卡级集群场景下更突破 60%。随着 GPU 计算性能持续提升、网络带宽增长相对滞后，通信瓶颈已成为制约系统扩展性、算力利用率与训练收敛速度的核心壁垒。

## 2. 核心目标
面向昇腾 NPU 集群（单机多卡/多机多卡），参赛团队需依托智能 Agent 技术，自主完成 HCCL 集合通信算法 (AllReduce/AllGather/ReduceScatter 等) 的全流程设计与代码开发。
* **性能要求**：实现高性能、高可靠、低延迟的分布式通信能力，深度适配昇腾兼容的高速硬件链路 (HCCS/ROCE/PCIe)。
* **技术成果**：提交完整的 Agent 技术成果，包括 Agent 能力清单 (Skills)、Prompt 工程设计方案及可独立运行的 Agent 工程。
* **验证环境**：允许通过模拟器完成成果验证，模拟器需精准模拟昇腾 NPU 集群的通信特性、拓扑结构及硬件参数。

## 3. 任务范围

### 3.1 通信原语要求
需覆盖昇腾 HCCL 核心集合通信原语（至少 3 种），且需通过智能 Agent 完成代码开发：
* **AllReduce**：用于分布式训练梯度同步（核心）。
* **AllGather**：用于模型并行参数聚合。
* **ReduceScatter**：用于数据并行分片归约。
* **Broadcast**：用于参数初始化广播。
* **AlltoAll**：用于序列并行/专家并行全局交换。

### 3.2 硬件与拓扑适配场景
* **单机多卡**：Full Mesh 全互联，适配全互联特性以发挥硬件带宽优势。
* **异构集群**：混合昇腾 910A2/910A3、不同代际 NPU、非对称带宽链路。
* **极限场景**：小数据块 (≤64KB)、大数据块 (≥1GB)、动态拓扑（节点增减）。

## 4. 关键技术要求

| 技术维度 | 昇腾集群通信专属要求 |
| :--- | :--- |
| **硬件感知** | 自动探测高速链路（HCCS/ROCE/PCIe）参数；感知 NPU NUMA 拓扑、HBM 分区、片上缓存 (UB) 容量。 |
| **算法创新** | 突破标准 Ring/Mesh/HD 算法局限，设计自适应拓扑算法（如 NHR 非均匀环、分块 Mesh、动态 Butterfly）。 |
| **软硬协同** | 利用硬件级通信原语、随路归约、计算通信并发调度，实现“零 CPU 介入、低延迟、高吞吐”。 |
| **可靠性** | 支持链路故障自愈、数据校验、超时重传、流量控制，避免集群“单点故障雪崩”。 |
| **可扩展性** | 算法复杂度控制在 O(N) 或 O(NlogN)，支持 8→1024 卡线性扩展，加速比 ≥90%。 |
| **精度保障** | 支持 FP16/BF16/FP32 混合精度通信，误差 ≤1e-6，无精度溢出/下溢。 |
| **Agent 技术要求** | 依托 Agent 完成全部算法设计与代码开发，提交 Skills 清单、Prompt 工程方案及完整 Agent 工程。 |

## 5. 开发约束
* **版本与接口**：基于昇腾 CANN 8.0 或之后版本，只能基于 HCOMM 开源仓（Gitee: ascend/cann-hcomm）提供的接口进行开发。
* **语言规范**：采用 C/C++ 语言开发，严格遵循 HCCL 插件接口（hcclCommInit/hcclAllReduce 等）。
* **底层红线**：严禁修改昇腾硬件底层驱动、高速链路固件，仅可基于官方 API 开发。

## 6. 参赛作品要求
1. **代码包**：包含 HCCL 算法插件 (.so)、头文件、CMake、测试用例、压测脚本、故障注入工具及 Agent 运行日志/Prompt 调用记录。
2. **技术文档**：包含算法设计说明书、性能测试报告（带宽/延迟/加速比）、可靠性报告、Agent 专项说明。
3. **演示材料**：5 分钟演示视频，展示算法原理、硬件协同机制、Agent 生成过程及模拟器/实机验证效果。
4. **Agent 专项输出物**：完整的 Agent 工程（含环境配置）、Agent 能力清单、Prompt 文件体系。

## 7. 评分标准
核心评判维度包括功能正确性、通信性能、算法创新性、工程化水平及 Agent 专项输出物的合理性。
* **正确性**：实现至少 3 种核心通信原语，结果与原生 HCCL 基准一致，无精度漂移。
* **兼容性**：在昇腾 910B/910C 或对应模拟器上正常编译运行，支持单机 8 卡及多机集群部署。
